#include<bits/stdc++.h>
using namespace std;
auto main()->int{
    vector<int> shoes(0);
    for(auto i=0;i<4;i++){
        int n;
        cin>>n;
        shoes.push_back(n);
    }
    for(auto i=0;i<4;i++){
        for(auto j=i;j<3;j++){
            if(shoes[i]!=shoes[j]) continue;
            else 
        }
    }
}